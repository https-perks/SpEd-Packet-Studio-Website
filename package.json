{
  "name": "sped-packet-studio-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "installer:update": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/update-installer.ps1",
    "installer:check": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/update-installer.ps1 -DryRun"
  },
  "devDependencies": {
    "vite": "^7.1.9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
